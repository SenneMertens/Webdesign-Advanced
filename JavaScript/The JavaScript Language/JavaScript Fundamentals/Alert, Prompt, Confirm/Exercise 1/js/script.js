"use strict";

let firstName, lastName;

firstName = prompt(`What's your first name?`, `First name`);
lastName = prompt(`What's your last name?`, `Last name`);

alert(`Your name is ${firstName} ${lastName}.`);